# R-1 engineering attack — Arrangement & Orchestration Brain at `origin/main` 4c5d967

Independent reviewer, round R-1 (engineering). Read-only. 2026-09-10.

## 0. Scope, checkout, method

- **Checkout.** `ws-brain-b09` is at `2ef26ab` (the B-09 branch head), not `4c5d967`. `git diff --stat 2ef26ab 4c5d967` is empty: the trees are byte-identical (`4c5d967` is the squash of `2ef26ab`). Everything below therefore holds for `origin/main`.
- **Paths.** All paths below are relative to `C:\Users\וינריך\Downloads\ai-music-platform\ws-brain-b09\`; `lib/` means `artifacts/api-server/src/lib/`.
- **What ran** (bundles prefixed `r1a-` under `.tmp-tests/`, results under the scratchpad `brain/` directory):
  - an import-graph / cycle / layering scan of `lib/` at HEAD and at `3bf23aa` (`r1a-graph.mjs`, the old tree extracted with `git archive`, no git state touched);
  - a timing + probe harness through `orchestrateArrangement` on the owner's fixture (Tier P) and the nine synthetic cases: reference composer, random-pitch composer, drums-only composer, the new B-05a/B-05b critics + judge on each result, a determinism check, and a 1×/2×/4× note-count scaling test (`r1a-timing-probes.jsonl`);
  - three follow-up probes (`r1a-probe2..4`): instrument-definition resolution, the owner's-song observations of the gated critics, the render attestation at the benchmark's own render options;
  - `pnpm --filter @workspace/api-server run benchmark -- --json --render --out …/r1a-bench.json` (3 m 34 s) and the deltas against both stored baselines; the CLI `--compare docs/evidence/benchmark-baseline/1467706.json --no-fail` run (see §7);
  - every bundle of the focused-runner groups `arrangement-brain`, `brain-merged`, `brain-b02/b04/b08/b10/b11`, `benchmark-corpus` (114 bundles) and part of `brain-invariants` (see §5).
- **Not verified**: anything requiring the database, the job runner end to end, the studio, or the workers; audio was rendered by the reference synth only; nobody listened.

## 1. Findings, ranked

### P0

**P0-1. The production job runner ignores the brain's hard-rule gate: a candidate the brain refused can be ranked first.**
`arrangementOrchestratorProvider.ts:443-456` throws only when *every* candidate failed; otherwise it returns the failed ones with `selectable: false` / `hardRuleFeasible: false` in `parameters` (`:513-517`, `:537-538`). Nothing downstream reads either field: `grep -rn selectable|hardRuleFeasible src` hits only `brainTelemetry.ts:33` (a copy into telemetry). The runner re-scores every candidate with `evaluateCandidateMusicalFit` (`arrangementGeneration.ts:1487-1491`) and the legacy PCM critic, ranks on `evaluationScore = (musicCritic.score + audioCritic.score)/2` (`:1626`, persisted as `score` at `:1803`), and `candidateRanking.ts` never consults the brain's verdict. B-08's own ledger says `candidateQuality` has 13 of 17 dimensions "insufficient data" on brain output (`docs/evidence/positive-control-ledger.json` → `lists.insufficient`). So B-00's "hard-rule failure blocks selection" is true inside `orchestrateArrangement` and false on the path a user reaches.
*Fix:* in `materializeCandidate`/ranking, drop (or hard-demote, `status: "rejected"`) any candidate whose `parameters.arrangementBrain.selectable === false`; add a runner-level test with a provider that returns one failed + one passed candidate. Owner: B-00 / B-13 (`arrangementGeneration.ts`, `candidateRanking.ts`).

**P0-2. The production composer receives none of the new global context: the default `composeParts` lambda drops it.**
`arrangementOrchestrator.ts:425-426`: `compose = input.composeParts ?? ((request) => composeReferencePart(request, { tempoBpm, meter }))`. The orchestrator does pass `{ decisions }` (`:533`) — but only an *injected* composer ever sees it; the default lambda forwards neither `context` nor `siblings` nor `originSeconds`. `ComposeContext` (`referencePartComposer.ts:33-45`) has no `decisions` field at all, so even a writer that wanted to register a voicing could not. Consequences, each verified in code:
- B-11 provenance for harmony / groove / register is structurally unreachable, not merely "not yet recorded" (`docs/brain/04-decision-provenance.md §6` promised "one line in `arrangementOrchestrator.ts`"; it was never added).
- B-02's sibling clearance never runs: `composer/harmonyParts.ts:150-176` falls back to a re-planned bass skeleton (`source: "planned"`) whenever `frame.siblings` is absent, which is always in production.
- B-10's ledger is never threaded: no non-test file assigns `motifLedger` (grep over `lib/`), so `composer/melodyParts.ts:41-56` builds a per-request local ledger and writes "recall across sections is unavailable" into it. The PR is titled "a thematic memory that writes"; on the production path the memory does not survive one part request.
- B-04's groove is not on the plan and not shared: `plan.groovePlan` is absent on the shipped plan (probe: `planKeys → groovePlan: "absent"`); `deriveGroovePlan` has no non-test caller; each kit/percussion/ostinato part re-derives its section groove (`composer/rhythmParts.ts:50-52` → `grooveSectionForRequest`, `groovePlan.ts:691-701`); `bassRhythmFor` / `compingRhythmFor` / `entryGestureFor` have **zero** callers, so the bass and comping never read the groove — B-04's own evidence records shipped kick/bass agreement 0.619 → 0.617.
- B-03's register plan is not applied: `registerBoundsFor` has no caller; `composer/harmonyParts.ts:118` carries it as a comment; the composer still uses the band-shift arithmetic of `composer/registers.ts:19-32`. `shouldRest` has no caller.
- B-09's grammar never reaches a writer: `composer/harmonyParts.ts:109-113` parameterises harmony from `productionAesthetic`, the style *string* and `styleFingerprint.harmonicComplexity`; the `StyleGrammar` slot exists only on the `contextAware` path, which the provider never enables (`arrangementOrchestratorProvider.ts:432-450` passes no `contextAware`), so `harmonyPlanFor` / `styleGrammarFor` / `composeWithContext` (`arrangementOrchestrator.ts:239-296, 475-496, 542-553`) are dead code in production and alive only in `tournamentProviders.ts:103`.
*Fix:* one integration change with a test per field: default lambda → `composeReferencePart(request, { tempoBpm, meter, originSeconds, siblings, decisions: ctx?.decisions })`; add `decisions?: DecisionRegistry` to `ComposeContext`; derive `plan.groovePlan` once per candidate and hand `GroovePlanSection` + the shared `GrooveHints` to the frame; thread `motifLedgerForPlan(...)` per candidate; call `registerBoundsFor` in `harmonyContext`; put the sectioned grammar on the V1 request. Owner: B-13 (the lead named it "playability unification + wiring"); the writers are B-02/B-04/B-10.

**P0-3. The instrument that ranks and gates in production is the one every ledger demotes; the new critics that would work are called by nobody.**
`arrangementOrchestrator.ts:58, 654, 841, 876, 899`: `critiqueArrangement` (music-critic/v1) computes `critique`, `finalScore` and the critic half of `hardRule`. B-08's merged ledger (`positive-control-ledger.json`, sha `5fea1b4`) demotes **all eleven** `musicCritic.*` dimensions; only `musicCritic.overall` gates, on `role_inversion`. Measured at HEAD through the production path (`r1a-timing-probes.jsonl`): a same-rhythm random-pitch composer is **selected** on pop-full (75 vs 77), ballad (76 vs 77), rock (71 vs 78), ethnic (74 vs 76), cinematic (71 vs 74) and orchestral (70 = 70, all five candidates eligible). On the other four cases it is refused only by the *hand-span / guitar-fingering* hard rule, not by any musical judgement. The drums-only composer is refused everywhere — by `dropped_part` findings, not by the critic (its score is 72–77, within a point of the reference).
The B-05a dimensions and B-05b adversarial modules, run on the same outputs, block the random composer on every case (5–24 blocking observations, `releasable: false`) and the reference composer on none of the nine synthetic cases — yet `critics/dimensions/index.ts` is imported by exactly one non-test file (`critics/controls.ts`), `critics/adversarial/index.ts` by two (an evidence module and its own evidence), `critics/judge.ts` by one. The DAG placed the wiring in B-06; B-06 is not merged; meanwhile selection, repair band and provider confidence all rest on v1.
*Fix:* rank on the judge over the new dimensions (with the ledger), keep v1 only as `initialCritique` for the drift metric — after P1-1 below is understood. Owner: B-06 (`criticRepairLoop.ts`, `arrangementOrchestrator.ts` critique/repair/select).

### P1

**P1-1. The gated critics were never run on the owner's song; when run, they block the reference output and nobody can say why.**
`evaluateAllDimensions` on the owner's fixture (no brief, 3 tracks, 2,093 notes): `groove = 0` (`off_grid` major ×8 and minor ×8, `subdivision_inconsistent` ×7, all on `bass-bass`, median |deviation| 8.7 ms, `offGridShare 0.235`, `toleranceMs 30`, `suspectedOrigin: perform`), `orchestration = 40` (`planned_family_silent` blocking in the 2-bar intro — the chord sheet starts at bar 3, B-01's known gap), verdict `releasable: false` with one gated blocking observation. On the nine synthetic anchors the same reference output scores 91.6–96.2 with no blocking. Either the groove tolerance is wrong for a real chord sheet (B-02's bass follows analysis chord onsets, which are not on the constant-tempo grid the composer assumes), or the performance engine's `phrase-legato` timing profile (see P2-4) is pushing a bass off-grid, or the critic is right. B-05a's tracker admits "no real arrangement — not רחם נא — has been critiqued yet". Charter rule 3 says name the cause only after a control isolates it; no such control exists.
*Fix:* add the owner's fixture as a tenth anchor in `critics/dimensions/anchors.ts`; run a control that quantises the bass onsets to the composer grid and one that removes performance timing; publish the result before P0-3 wires anything. Owner: B-05a + B-04.

**P1-2. Eight of sixteen `critics/dimensions` suites fail on main now (13 tests), and the tracker does not say so.**
Run at HEAD (`r1a-suites-C.log`, per-bundle logs `.tmp-tests/r1a-lib-critics-dimensions-*.log`): `harmony` (null control: `orchestral-midi: 89.2` < 90), `groove` (prepared-fill positive control), `orchestration` ×2 (`tutti_everywhere` "detected 2/5"; the "before/after the merge" anchor test), `register` (`TypeError: Cannot read properties of null (reading 'input')` — the purpose-built control can no longer be applied), `density` (`chorus_thinner_than_verse`), `transitions` ×2 (`dance-full+realise_boundaries`), `repetitionVsVariation` (dance breakdown), `sectionDevelopment` ×3 ("pop-full: chorus 2 is still recognisably the chorus"). Plus `tournamentArms` (57 ≠ 60). `critics/controls.test.ts` passes (5/5) because it only checks that the committed ledger equals a regeneration, blocking counts and confidence bounds — not the per-dimension assertions. These suites are registered under the **`benchmark-corpus`** group of `scripts/run-focused-api-tests.mjs`, not under any `brain-*` group, so B-02 ("31 chain suites green") and B-04 (which lists only adversarial/judge failures — now green, 43/43 and 10/10) never ran them, while both regenerated `critics/dimensions/controlLedger.ts` (git log: touched by B-02, B-04, B-05a).
*Fix:* move the sixteen suites + `controls.test.ts` into `arrangement-brain`; fix or re-anchor the 13 tests; make "all groups green" a merge gate the lead runs, not a claim. Owner: B-05a + lead.

**P1-3. Three positive-control ledgers, three rules, and "gated" statuses that rest on the author's own damage.**
- B-05a `critics/controls.ts:19-24, 293-300`: gated = strongest claimed control ≥ 0.90 with CI lower ≥ 0.60 and no blocking on clean anchors.
- B-05b `critics/adversarial/controls.ts:354-364`: gated = every anchor caught **and CI lower > 0.5** — 7/7 gives 0.59, "gated" under B-05b and "informing" under B-05a. Six of eight adversarial modules are "gated" on that weaker rule.
- B-08 `positiveControlLedger.ts:14-17`: gate = ≥ 90 % at the strongest rung on ≥ 8 trials; that ledger is at sha `5fea1b4`, before B-02/B-04/B-09/B-10 changed the composer four times, and was not re-run.
- Prepared controls: `transitions` is gated on `erase_boundary_events+realise_boundaries` and `sectionDevelopment` informs on `chorus_copy+develop_chorus_2` (`controls.ts:52-58`, `anchors.ts:704-706, 748-750`): the harness first *writes* fills/pickups/development the composer never wrote, then erases them and counts the erasure as detected. That measures the critic's sensitivity to the harness's own gesture, which is fine as a unit test and wrong as a `gated` status. `playability` and `voiceLeading` are both gated on the single transform `bass_roots_only_leaps` (`controlLedger.ts`).
- The ledger is generated by the code it certifies (`renderLedgerSource`, `controls.test.ts:124-169`) and the evidence JSON omits 616 per-item results "regenerated by the harness" (`brain-b05a-critic-controls.json → itemsOmitted`).
*Fix:* one rule (`SENSITIVITY_GATE`) and one ledger module for all three; a prepared control may yield `informing`, never `gated`; require two independent transforms per gated dimension. Owner: B-08 (rule) + B-05a/B-05b.

**P1-4. The repair stage is still dead in production, and the charter's confidence formula counts it.**
At HEAD, 50 candidates (owner + 9 cases × 5): 43 attempted a repair pass, **0 applied** (rock-full and cinematic did not even enter the band). B-00 reported 0/36 and called it "BENCHMARKED as a null result"; four composer streams later it is still 0. `brainConfidence` (`arrangementOrchestratorProvider.ts:193-215`) nonetheless spends 0.15 weight on `agreement` and 0.15 on `intact`, and its 0.35 `coverage` term is a weighted mean of `musicCritic` per-dimension confidences that are literals (`musicCritic.ts:185, 204, 241, 267, 291, 316, 325, 359, 397, 434, 446, 458, 471, 493, 516, 535`, capped at 0.4 when no note was consulted). The "evidence-derived" confidence is a formula over constants with a documented shape — better than `0.5 + score/200`, not evidence.
*Fix:* until B-06 gives `applyPlanRepairs` operators that fire, mark the repair stage `skipped: "no operator applies"` and remove `agreement`/`intact` from confidence, or derive `coverage` from the new critics' `coverage` fields. Owner: B-06 / B-00.

**P1-5. `renderFailures = 5 on every case` in all three baselines is the benchmark rendering at 24 kHz and its own attestation rejecting 24 kHz.**
`arrangementBenchmark.ts:248` passes `renderOptions: { sampleRate: 24_000, bitDepth: 16 }`; `renderArrangementStems` at those options fails `correct_sample_rate: 24000 Hz` on every stem (probe `r1a-probe4`); at the orchestrator's default 48 kHz the same tracks are feasible and the audio critic scores 92. B-08 wrote "recorded, not investigated — outside B-08's files" twice (tracker 7064, 7066). The audio score that weighs 0.4 of `finalScore` in render runs was therefore always computed on renders the pipeline itself declares infeasible.
*Fix:* render the benchmark at 48 kHz (or make the check option-aware) and re-snapshot. Owner: B-08.

**P1-6. Two planners still run per job; the persisted plan is a legacy skeleton with brain layers grafted on.**
`arrangementGeneration.ts:393-410` still calls `buildArrangementBrain` + `createArrangementPlan` for every brain candidate, then `adoptBrainPlan` (`:448`) overwrites six layers and the sections but keeps "style, hierarchy, composition intelligence, track directives" from the legacy planner (`brainPlanAdoption.ts:20-26`). `createStyleSpec` (`:381`) is still called with the style string, not the brief grammar B-09 put on `plannerHints.global.styleGrammar` (B-09 listed this as a change it does not own). Result: a stored arrangement carries a `StyleSpec` resolved from a string and an arc resolved from the brief — two style answers on one row.
*Fix:* pass the brief grammar as `createStyleSpec`'s fourth argument; skip the legacy planner entirely when `readArrangementBrainEvidence` succeeds and synthesise only the fields export/selection read. Owner: B-00 / B-09.

**P1-7. The capability ledger is not the ledger.**
`docs/brain/03-capability-ledger.md` ("updated by the lead at every merge") was updated by B-05a and B-11 only (`git log`); after ten merges it still reads Arrangement arc **D**, form memory **D**, harmony realisation **I**, one chord parser **—**, instrument profiles **I (7 substring definitions)**, register plan **D**, GroovePlan **—**, transitions **D**, motif ledger **—**, style intelligence **I**, property tests **—**, frozen benchmark **—**, baseline **—**, real corpus **—**, "chain suites registered: partial (14 missing)". Each PR keeps its own "Capability ladder" paragraph instead, with its own wording ("INTEGRATED ✓ (local ledger on the production path)" for a memory that does not persist — B-10; "GroovePlan INTEGRATED ✓ (every kit/percussion/ostinato part derives it; not yet persisted)" for a plan the bass never reads — B-04). Two ledgers, and the authoritative one is stale.
*Fix:* the lead updates one table per merge or deletes the file; ladders in tracker entries reference rows in it. Owner: lead.

**P1-8. A 24-module import cycle was created, and the 5.4 k-line engine now loads the genre knowledge base.**
At `3bf23aa` the largest cycle was 3 modules (`candidateStrategies ↔ musicEngines ↔ partComposer`). At HEAD one strongly connected component holds 24 modules: `musicEngines → styleResolver → styleKnowledge/index → 14 genre entries → styleKnowledge/schema → styleGrammar`, `musicEngines → globalArrangementPlanner → styleResolver → styleGrammarResearch → styleGrammar`, closed back through `styleGrammar → partGenerationContextV2 → partComposer → musicEngines` (the last two edges are type-only, the rest are runtime). `createStyleSpec` (`musicEngines.ts:430-442`) now calls `resolveStyle`, so the export/mix engine module transitively imports every genre file. Layering hits (graph scan): `composer/harmonyParts → arrangementArc, musicEngines`; `transitionRealisation → arrangementArc`; `partComposer → musicEngines, globalArrangementPlanner, sectionPhrasePlanner, arrangementArc`; `critics/dimensions/shared → globalArrangementPlanner`; `critics/*/anchors → arrangementOrchestrator, referencePartComposer, benchmarkCorpus` (test fixtures living in production directories); five evidence modules (`brainB01/02/04/09/10Evidence.ts`, 1,694 lines) sit in `lib/` and import the orchestrator, the composers and the critics.
*Fix:* move `resolveStyle` out of `musicEngines` (an adapter module the runner imports); move `brainB*Evidence.ts` and `critics/**/anchors.ts` under `scripts/` or `__fixtures__/`; give `canonicalFamily` its own module so composers stop importing the arc planner. Owner: B-09, B-11, lead.

**P1-9. The golden pin pins nothing across streams; the benchmark baseline was not re-snapshotted; the B-08 ledger is stale.**
`__fixtures__/reference-part-composer.golden.json` was re-pinned by B-01, B-02, B-03 and B-04 (git log) — each stream re-pins to its own output, so the "byte-identical golden" guards only the mechanical split inside B-00. B-08's protocol ("re-snapshot when the shipped notes change") was not followed after B-02/B-04/B-10 changed every composer digest; `docs/evidence/benchmark-baseline/` still ends at `1467706.json`, and my run is the first measurement of the merged composer (§7). `positive-control-ledger.json` is at `5fea1b4`.
*Fix:* re-snapshot + re-run the ledger at every composer change as a merge step; keep the golden as "drift reported" (B-12 style) rather than re-pinned. Owner: B-08 / lead.

**P1-10. Two definitions of "the metre is right": B-04 certified its own; the independent B-12 invariant still fails 0/20 on 5/4, 6/8 and 7/8 at HEAD.**
B-04's tracker: "re-metred variants correct 3/7 → 7/7 — … 6/8 … → snare on unit 3, kick 0 and 2; 7/8 … → snare 4, kick 0 and 2 (2+2+3)". `invariants/meter.property.test.ts` run at HEAD (`.tmp-tests/r1a-lib-invariants-meter-property-test-ts.mjs.log`): `6/8: 0/20 pass; codes {kick_missing_downbeat: 20, compound_pulse_ignored: 20, pattern_period_not_bar: 18, snare_on_downbeat: 10}` — e.g. seed 201 "kick on the downbeat in only 13 % of 32 bars | 59 kick/snare hits on eighths other than 1 and 4 (dotted pulses)"; `7/8: 0/20 {kick_missing_downbeat: 20, pattern_period_not_bar: 12}`; `5/4: 0/20 {kick_missing_downbeat: 20, pattern_period_not_bar: 17}`; `3/4: 16/20`. B-04's kit template places the 6/8 kick on eighths 1 and 3; the compound pulses are eighths 1 and 4 — B-04 measured "correct" against its own cell. The `todo` reason strings in the B-12 suite are stale (they still cite the pre-B-04 bar-length cause), so the suite's own text no longer explains its failure; the `ℹ` diagnostics do. "Kick on the downbeat in 13 % of bars" also points at `applyDensity` stride thinning (P2-5) and the kit's "breath at phrase ends".
*Fix:* make B-12's metre checker the acceptance test for B-04's templates (or argue in writing why eighth 3 is a pulse in 6/8); refresh the todo reasons. Owner: B-04 + B-12.

### P2

**P2-1. Concurrency: `orchestrateArrangement` is synchronous CPU work inside the API process.** The owner's song takes 4.5 s for five candidates (0.77 s for one) with render off; during that window no heartbeat (`arrangementGeneration.ts:1100`), cancellation watcher (`:1117`) or HTTP request runs. The lease is 2 min (`:1035`), so no lease loss at today's sizes, but a longer song or the planned in-process render (B-07) would starve them; only `listeningRenderOffThread.ts` uses a Worker. Not a defect today; a ceiling.

**P2-2. Determinism is fine on the brain path; the fragile spots are elsewhere.** Same input twice → identical track models and plan (verified); a wall-clock `now` changes only `derivedAt` fields, not notes (verified); the provider pins `new Date(0)`. `deriveGroovePlan` falls back to `new Date()` (`groovePlan.ts:744`) — moot while nothing calls it. `registerOf` (`composer/registers.ts:34-39`) breaks ties on `Object.entries` insertion order; `dedupeSimultaneous` keys on `Math.round(start*200)`. B-08's ledger counts depend on anchor order (shared corruption RNG) — admitted, unfixed.

**P2-3. Style: six concepts where the charter asked for one.** Two exported types named `StyleGrammar` coexist — the Q-02 slot in `lib/db/src/schema/music-studio.ts:2161-2174` (eight vocabulary words) and the B-09 contract in `styleGrammar.ts:250-282` — beside `StyleSpec` (`:2175`), `StyleProfile` (`:3820`), `PerformanceStyle` (`:3407`) and `UniversalStyle` (`universalStyleSchema.ts:90`, zero importers). B-09 admits two grammars per job (brief on the planner, fingerprint on the slot). Two request contracts remain (`PartGenerationRequest` V1 and `upgradePartGenerationRequest` V2) and two voicing solvers (`voiceLeading.solveVoiceLeading` SATB on the dead `contextAware` path; `harmonyPlan/voicings.planVoicings` on the live one). Six "reason" vocabularies now exist (`ArcDecision`, `PartComposerPlan.decisions`, groove `source/reason`, grammar `provenance`, register-plan `reason`, `DecisionRecord`); `buildCandidateProvenance` converts three.

**P2-4. Survived instrument defect: an electric bass is a bowed string to the performance layer.** `instrumentProfile.ts:563` keeps `family: "strings"` for `electric_bass` (copied from the legacy `musicEngines.ts` at `3bf23aa:416-419`); `getInstrumentPerformanceCapability` then hands the bass `bowed-and-pizzicato / phrase-legato / continuous-bow-expression`, and the shipped `performanceEvidence.instrumentFamily` is `strings` on every case (probe `r1a-probe2`). The B-11 tracker's "keys still resolves to the drum kit" is stale in the other direction: `keys`/`piano` in `RHYTHMIC_HARMONY` resolve to the keys profile at HEAD.

**P2-5. F7 survived untouched.** `applyDensity` (`arrangementOrchestrator.ts:303-319`) is still stride thinning; B-12's C7 (deletes downbeat kicks 16/20 in 3/4) and B-10's own finding (a 10-note counter-line thinned to 5) both trace to it; `candidateStrategies.ts`, `candidateDiversity.ts`, `candidateQuality.ts` are byte-unchanged since `3bf23aa`.

**P2-6. Ownership rule broken where it matters.** "Two agents never edit the same file" — `music-studio.ts` was edited by seven brain PRs (4,285 → 5,331 lines), `run-focused-api-tests.mjs` by all of them (and was corrupted twice: hotfix `8de7818`, B-09's repair), `referencePartComposer.ts` by four, `partComposer.ts`, `composer/harmonyParts.ts`, `composer/frame.ts`, `arrangementOrchestrator.ts`, `arrangementGeneration.ts` by three each.

**P2-7. God-file growth moved rather than shrank.** `musicEngines.ts` 5,402 → 5,386; `arrangementOrchestrator.ts` 622 → 1,007; `arrangementOrchestratorProvider.ts` 299 → 554; `partComposer.ts` 385 → 615; `orchestrationBudget.ts` 313 → 776; `styleGrammar.ts` 307 → 912; `globalArrangementPlanner.ts` 470 → 648; `sectionPhrasePlanner.ts` 459 → 616; `arrangementBenchmark.ts` 341 → 586; `referencePartComposer.ts` 325 → 141 (the one real split). New: `realCorpusTierH.ts` 2,167 (data), `instrumentProfile.ts` 1,551, `arrangementArc.ts` 907, `groovePlan.ts` 814, `melodicEngine.ts` 704, `styleResolver.ts` 702.

**P2-8. Fourteen suites are still unregistered** (different fourteen from the audit's): `arrangerModelProvider`, `arrangerTrainingPipeline`, `loudness`, `masteringEngine`, `mixAutomation`, `mixBrain`, `nativeRendererAssets`, `pairwiseCritic`, `personalProfile`, `preferenceEvents`, `premiumInstrumentRouting`, `producerDecisionLedger`, `producerIntelligence/producerLanguage`, `providerReliability`. And the registered runner does not run on the owner's machine: `node scripts/run-focused-api-tests.mjs brain-merged` → `spawn esbuild ENOENT`, exit 1, while `run-benchmark.mjs` works because it uses esbuild's Node API.

## 2. Architecture: duplicated concepts, cycles, layering (question 1)

Covered by P1-8, P2-3, P2-6, P2-7. Additional specifics:
- **Two frames**: `ComposeFrame` (`composer/frame.ts:43-66`) and `HarmonyFrame` (`composer/harmonyParts.ts:63-70`, adds `window` and `siblings` "without editing frame.ts") — writers cast `frame as HarmonyFrame`; the window is recomputed in `windowOf` when the cast fails.
- **Chord parsers**: B-02 delivered `chordSymbols.ts`; still parsing on their own at HEAD: `musicCritic.ts:99`, `partJudge.ts:154`, `symbolicCorruptions.ts:186`, `harmonyEngine.ts:460` (adapter), `analysisGold.ts:299`, `analysisDisagreement.ts:178`, `analysisProviders.ts:1234`, `export-pipeline.ts:313`, `invariants/generators.ts:481`. The production critic and the corruption library that certifies the critics still read chords differently from the composer.
- **Provenance registries vs decisions on the plan**: `PartComposerPlan.decisions` (B-01) and `ArrangementArc.sections[].*` decisions live on the plan; B-11's `DecisionRegistry` lives per candidate and is rebuilt from the plan by `buildCandidateProvenance`; the composer's registry is empty in production (P0-2).

## 3. Fake intelligence (question 2)

P0-3, P1-1, P1-3, P1-4, P1-5, P1-7, P2-4. Also:
- **Constants presented as judgement** are at least labelled: B-04 groove ceilings, B-05a thresholds, B-09's 14 hand-written genre entries, B-03 conventions — every honest-limits section says so. What is *not* labelled: `finalScore = 0.6·symbolic + 0.4·audio` (`arrangementOrchestrator.ts:903`) with an audio critic B-08 shows scores 85–92 on everything and a render the attestation calls infeasible (P1-5).
- **`controlStatus` a trivial control satisfies**: `emotionalArcAndTension` is gated on `flatten_arc` ("every velocity set to the song mean and every part that does not play in the quietest section deleted from the climax", `anchors.ts:503-505`) while the dimension's energy proxy is 0.3·onsets + 0.3·velocity + 0.25·parts + 0.15·spread (`emotionalArcAndTension.ts:66`); `performanceRealisation` is gated on `strip_cc_and_quantise` while it measures velocity stddev and CC count (`performanceRealisation.ts:39-53`). These controls are the detector's definition inverted; they prove the code runs, not that the dimension hears music.
- **Tests that pin the implementation**: `brainB02Harmony.test.ts` ("against the recorded before numbers"), the four golden re-pins, `controls.test.ts:167` (committed ledger == regenerated ledger), B-12's `golden.property.test.ts` (digests). `arrangementBrainIntegrity.test.ts` pins that random pitches "still pass the hard rules — reported as such, not tuned" (B-00 tracker): a test that asserts nonsense passes.

## 4. Race conditions, determinism, concurrency (question 3)

P2-1, P2-2. No `Math.random` on the brain path; `Date.now()` only in the ledger's timing and the health caches (`musicEngines.ts:3912-3921`, `musicProviders.ts:1450-1621`); `new Date()` unpinned in `arrangementGeneration.ts` lease/heartbeat fields only. No worker-thread plan exists for orchestration or the B-07 render; `sharedSmoke` is a module singleton (fine).

## 5. Test blind spots (question 5)

- **Fails on main right now** (114 bundles run by hand, node 24): 8 `critics/dimensions` suites / 13 tests + `tournamentArms` 1 (P1-2). Everything in `arrangement-brain` (19 bundles), `brain-merged`, `brain-b02/b04/b08/b10/b11` passes. `brain-invariants`: `generators` 4/4; `transposition` 3 pass / 1 todo (7/24 seeds — C5 still open); `tempo` 2 / 1 todo (13/24 — C3 open); `sectionNaming` 4 / 2 todo (0/20 Hebrew and unnamed — C9 open: `classifySection` is an English regex, `globalArrangementPlanner.ts:63-74`, on a Hebrew owner's product); `meter` 2 pass / 4 todo-failing (P1-10); `instrumentSwap` 2 pass / 2 todo, of which the "definition belongs to its planner family" todo now **passes** (B-03 fixed C1 — the suite's reason text is stale) and the keys→guitar swap still fails (C12); `golden` 3/3 (drift reported, not blocked, so it passes by design after four composer changes); `determinism` 4 pass / 1 todo-failing — the "different seed" test fails for a cause other than the one its stale reason names (C1 is fixed; the current diff is unexplained); `emptyParts` 2 pass / 1 todo that now **passes** (`24/24 pass; 0 silent pairs out of 471 planned` — C10, the silent LEAD, is closed by B-01; the reason text still says "12/24"); `scopePreservation` 2 pass / 1 todo that now **passes** (20/20; C6's seed-803 duplicate id no longer reproduces on the new composer — nobody fixed `candidateRepair.ts`, the input changed); `fuzz`, `playability`, `criticSanity`: still running when this report was written — results, if finished, are in `r1a-suites-B.log` and the matching `.tmp-tests/r1a-lib-invariants-*.log`. Node's reporter marks a `todo` whose assertion still fails with `✖` and one that now passes with `✔ … # TODO`; every count above was read from those markers, not from the reason strings.
- **Unregistered**: P2-8. **Runner unusable locally**: P2-8.
- **Which new tests would pass on a composer that returned nonsense** through the production selection path: all of `arrangementOrchestrator.test.ts`, `arrangementOrchestratorProvider.test.ts`, `brainPlanAdoption.test.ts`, the B-11 suites and the benchmark suite — none asserts that the selected candidate is musically better than a random one; `arrangementBrainIntegrity.test.ts` asserts the opposite is *reported*. The only tests that reject nonsense are the B-05a/B-05b suites, on modules production does not call (P0-3).

## 6. Performance (question 6)

Measured at HEAD, render off unless stated (`r1a-timing-probes.jsonl`):
- Owner's song (141 bars, 22 part tasks): **4.46 s** for 5 candidates, 0.77 s for 1. Attribution: composer 3.28 s (73 %; 110 calls ≈ 30 ms each, the B-02 chain solver + B-04 kit), planning chain 34 ms, `critiqueArrangement` 14 ms per call (×2 per candidate + repair loop), constraints 5 ms, provenance 2 ms, new critics + judge 174 ms total.
- Nine synthetic cases: 0.16–1.18 s each for 5 candidates; the same shape (compose 40–85 %).
- With render (benchmark): **23.6 s mean per case**, i.e. ~95 % is the reference synth + audio critic (one pop-full render pass = 19.7 s for one candidate set). B-08's 35.7 s → 24.8 s → 23.6 s is render, not brain.
- Scaling (owner tracks ×1/×2/×4 = 2,093 / 4,186 / 8,372 notes): `critiqueArrangement` 37 / 55 / 173 ms, constraints 18 / 25 / 85 ms, sixteen dimensions 90 / 124 / 298 ms, adversarial 36 / 42 / 128 ms — near-linear; nothing quadratic found up to 8.4 k notes. Potentially quadratic but not on the live path: `bassReference` sibling scan (events × notes) and `harmonyPlanFor`'s `bars.find` per chord.
- Waste: the five strategies recompose all 22 parts with identical inputs except seed/density; a (task, seed) cache would remove ~60 % of the symbolic time.

## 7. Regressions and the benchmark (question 7)

Stored snapshots: `3bf23aa.json` (pre-program) vs `1467706.json` (B-00/B-01/B-03/B-05b/B-12 merged): critic 74.56 → 73.5, shipped 74.67 → 73.5, harmonyScore 98.43 → 97.78, clash 0.48 → 0.81, trajectory 48.03 → 76.44, seams 50.7 → 43.64, motif 33.16 → 34.57, notes 557 → 467, unselectable — → 11.11 (ethnic-vocal), latency 35.7 s → 24.8 s; per case, playability 82 → 0 on cinematic's shipped critique and 64 → 0 on dance's — noted by nobody.

**My run at HEAD** (`r1a-bench.json`, render on, 9 × 5, same metric pins and corpus as `1467706.json`), Δ vs 1467706:

| metric | 3bf23aa | 1467706 | HEAD | Δ | explained by a stream? |
|---|---|---|---|---|---|
| criticScore / shipped | 74.56 / 74.67 | 73.5 / 73.5 | **75 / 75** | +1.5 | no compare was run by B-02/B-04/B-09/B-10 |
| harmonyScore | 98.43 | 97.78 | 97.62 | −0.16 | — |
| clashShare (↓) | 0.48 | 0.81 | **0.95** | +0.14 | B-02 wider voicings? not measured by B-02 |
| trajectorySmoothness | 48.03 | 76.44 | **67.06** | −9.38 | ballad −40 (strategy melodic → sparse, 434 → 198 notes), cinematic −18; unexplained |
| seamArtefacts | 50.7 | 43.64 | 57.51 | +13.87 | B-08 says the component detects no corruption family |
| motifRecurrence | 33.16 | 34.57 | 32.47 | −2.10 | dance +90, acoustic −40, jazz −40: selection noise |
| candidateDistance | 6.59 | 6.69 | 8.16 | +1.47 | — |
| playabilityErrors | 0 | 0 | **0.33** | +0.33 | rock-full 0 → 3; no stream reports it |
| unselectableShare | — | 11.11 | **0** | −11.11 | B-04's meter fix (ethnic-vocal 7/8) — explained |
| audioScore | 90.33 | 89.63 | 89 | −0.63 | on renders the attestation rejects (P1-5) |
| renderFailures | 5 | 5 | 5 | 0 | P1-5 |
| noteCount / latency | 557 / 35.7 s | 467 / 24.8 s | 475 / 23.6 s | | |

Selected strategy changed on 5 of 9 cases (ballad melodic→sparse, rock sparse→melodic, acoustic conservative→sparse, jazz sparse→rhythmic, ethnic none→conservative); most metric movement follows the selection, not the composer — the measure is selection-sensitive by construction.

**CLI verdict** (`pnpm … run benchmark -- --render --compare docs/evidence/benchmark-baseline/1467706.json --no-fail`, log `r1a-bench-compare-1467706.log`): **`DO_NOT_PROMOTE: REFERENCE_PIPELINE regresses trajectorySmoothness, motifRecurrence`** — improved: criticScore +1.5, shippedCriticScore +1.5, seamArtefacts +13.87, candidateDistance +1.47, unselectableShare −11.11; regressed: trajectorySmoothness −9.38, motifRecurrence −2.1; "unchanged": audioScore −0.63, harmonyScore −0.16, clashShare +0.14 and **playabilityErrors 0 → 0.33** (rock-full's three errors sit inside the comparer's tolerance and are reported as *unchanged* — a quality metric that was 0 on every baseline and is not 0 now should not be tolerated away). The CLI re-run reproduced every quality aggregate of my first run to the second decimal (latency doubled only because four other jobs shared the CPU), which independently confirms that selection and measurement are deterministic on this brain. So: the merged composer is DO_NOT_PROMOTE against the last snapshot by the program's own rule, four composer streams merged without running that compare, and the rock-full playability regression has no owner.

## 8. The ten things I would fix first

1. Runner honours `selectable: false` (P0-1) — one guard in `materializeCandidate`/ranking plus a runner test.
2. Wire the composer context in the default lambda and `ComposeContext` (P0-2): siblings, decisions, originSeconds, groove plan on the plan, motif ledger per candidate, `registerBoundsFor`, grammar on the request — with a test per field that fails when the field is dropped; and make B-12's metre invariant the acceptance test for B-04's kit templates before the bass and comping start reading them (P1-10).
3. Run the new critics on the owner's song, resolve `groove = 0` with two isolating controls (P1-1), then make the judge the ranker and gate (P0-3) and demote music-critic/v1 to `initialCritique`.
4. Fix the 13 red tests, move the critics suites into a brain group, make the focused runner use esbuild's Node API so the owner's machine can run it (P1-2, P2-8).
5. One control rule and one ledger; prepared controls never gate; two transforms per gated dimension (P1-3).
6. Benchmark at 48 kHz, re-snapshot at HEAD, re-run the positive-control ledger, explain rock-full's 3 playability errors (P1-5, P1-9, §7).
7. Repair: give `applyPlanRepairs` operators that fire or mark the stage skipped and take it out of confidence (P1-4).
8. Break `musicEngines → styleResolver/knowledge base`; move evidence modules and critic anchors out of `lib/` (P1-8).
9. One capability ledger, updated per merge, or none (P1-7); stop re-pinning the golden.
10. Electric bass profile family/performance capability, `applyDensity` musical thinning, `classifySection` for Hebrew section names (P2-4, P2-5, C9).

## 9. Not verified

Job runner against a database; studio trace page; export/native render; B-13/B-06/B-07 worktrees (out of scope); the `music-engines` and other non-brain runner groups; invariants listed as pending above; any human judgement of any output.
